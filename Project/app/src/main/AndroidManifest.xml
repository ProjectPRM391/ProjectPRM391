<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.project">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".CustomerProfile"></activity>
        <activity android:name=".PostChapterActivity"></activity>
        <activity android:name=".ChapterActivity" />
        <activity android:name=".PostActivity" />
        <activity android:name=".ListCustomer" />
        <activity android:name=".Home" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Register" />
        <activity android:name=".LoginForm">

        </activity>
        <activity android:name=".ListStoryAdmin" />
        <activity android:name=".MainActivity" />
        <activity android:name=".MyActivity" />
        <activity android:name=".ShowStory"/>
    </application>

</manifest>